udp
发送数据的流程
创建套接字——发送数据——关闭套接字

接收数据的流程
创建套接字——绑定本地自己的信息(ip和port)——接收数据——关闭套接字

tcp客户端
创建套接字——连接服务器(connect)——发送接收数据(send/recv)——关闭套接字

tcp服务器
创建套接字——绑定端口(bind)——使套接字变为可以被动连接(listen)——等待客户端的链接(accept)——接收发送数据(send/recv)——关闭套接字

